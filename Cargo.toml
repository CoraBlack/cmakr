[package]
name = "cmakr"
description = "Build dependence for build cmake project in build.rs"
authors = ["Cora"]
license = "MIT"
version = "0.1.0"
edition = "2024"

[dependencies]
which = "8.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"